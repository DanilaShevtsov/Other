#include <iostream>
#include <stdio.h>

int main()
{
	int n = 0, i = 0;
	char str[200];
	 
	gets_s(str);
	while (str[i] != '\0') {
		if ((str[i] != ' ') && (str[i] != '	')) {
			printf("%c", str[i]);
			i++;
		}
		else {
			n++;
			i++;
		}
	}
	printf("\n%d", n);
}
