#include <iostream>
using namespace std;

int main() {
	int n, k, j = 0;
	cin >> n >> k;
	int *arr = new int[n];

	for (int i = 0; i < n; i++) {
		cin >> arr[i];
	}

	for (int i = 0; i < n; i++) {
		if (arr[i] == k)
			++j;
	}

	cout << endl << j;
}
